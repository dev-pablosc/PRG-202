// Crear arrays(2)

// Uso de corchetes []
let numeros = [1, 2, 3];
console.log(numeros)

// 2da Manera () >> no tan comun
const dias = new Array('lunes', 'martes', 'miercoles')
console.log(dias)
console.table(dias)

// Array tiene datos variados
const variado = ['Hola', 14, true, null, undefined, {nombre: 'Pablo', lenguaje: 'JS'}, ['a', 'e', 'i']];
console.log(variado)
console.table(variado)