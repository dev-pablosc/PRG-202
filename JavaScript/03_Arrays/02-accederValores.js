// Acceder a los valores del Array
const numeros = [1, 2, 3]; 
console.table(numeros);

console.log(numeros[2]);

const variado = [10, 20, 30, ['a', 'e', 'i']];
console.log(variado[3])

// Valores dentro de arrays
console.log(variado[3][1])